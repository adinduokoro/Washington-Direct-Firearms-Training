import React from "react";
import styles

const Layout = () => {
  return (
    <div>
      <div className={style.header}>{/* Header */}</div>
      <div>
        {/* Outlet */}
        {/* Footer */}
      </div>
    </div>
  );
};

export default Layout;
